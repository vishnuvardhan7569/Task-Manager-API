#!/usr/bin/env ruby
# frozen_string_literal: true

# The `kamal` gem is not installed in this environment.
# Install `kamal` (add it back to your Gemfile) to enable deploy tooling.
puts "kamal is not available. Install the 'kamal' gem to enable deployment commands."
exit 0
