#!/usr/bin/env ruby
# The `thruster` gem is not installed in this environment.
# Install `thruster` (add it back to your Gemfile) to enable the puma acceleration tool.
puts "thruster is not available. Install the 'thruster' gem to enable the puma acceleration tool."
exit 0
